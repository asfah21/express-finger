fastapi==0.115.6
uvicorn[standard]==0.34.0
numpy==2.2.2
opencv-python-headless==4.11.0.86
# CPU-only runtime: the service does not use NVIDIA CUDA inside the container.
# Keep these packages pinned so pip does not resolve GPU-enabled variants.
ultralytics==8.3.63
insightface==0.7.3
onnxruntime==1.20.1
